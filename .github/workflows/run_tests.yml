name: Run Tests

on:
  workflow_call:
    secrets:
      SLACK_WEBHOOK_URL:
        required: true
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true
      CONTRIBSYS_REPO_USERNAME:
        required: true
      CONTRIBSYS_REPO_PASSWORD:
        required: true

env:
  GIT_HASH: ${{ github.event.pull_request.head.sha || github.sha }}
  GIT_REF_NAME: ${{ github.event.pull_request.head.ref || github.ref }}
  SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  POSTGRES_PASSWORD: mysecretpassword
  POSTGRES_USER: postgres
  POSTGRES_DB: postgres
  MINIO_ADDRESS: http://localhost:9005
  MINIO_KEY: minioadmin
  S3_BUCKET: mint-app-file-uploads

jobs:

  build_docker_images:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        service_name:
          - db
          - db_migrate
          - mint
          - mint_client
          - minio
          - minio_mc
          - email
          - faktory
    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          ref: ${{ env.GIT_HASH }}
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: docker.contribsys.com
          username: ${{ secrets.CONTRIBSYS_REPO_USERNAME }}
          password: ${{ secrets.CONTRIBSYS_REPO_PASSWORD }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Run buildx bake
        uses: docker/bake-action@v2.3.0
        with:
          files: "docker-compose.yml,docker-compose.override.yml"
          # targets: "${{ matrix.service_name }}"
          load: true
          set: |
            ${{ matrix.service_name }}.cache-from=type=gha,scope=${{ env.GIT_REF_NAME }}-${{ matrix.service_name }}
            ${{ matrix.service_name }}.cache-to=type=gha,mode=max,scope=${{ env.GIT_REF_NAME }}-${{ matrix.service_name }}

  server_test:
    if: ${{ false }}
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:14.4
        env:
          POSTGRES_DB: ${{ env.POSTGRES_DB }}
          POSTGRES_USER: ${{ env.POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ env.POSTGRES_PASSWORD }}
        ports:
          - 5437:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 1s
          --health-timeout 1s
          --health-retries 5

      minio:
        image: minio/minio:edge-cicd
        ports:
          - '9005:9000'
        options: >-
          --health-cmd "curl -s http://localhost:9000/minio/health/live"
          --health-interval 1s
          --health-timeout 1s
          --health-retries 5

      faktory:
        image: docker.contribsys.com/contribsys/faktory-ent:latest
        credentials:
          username: ${{ secrets.CONTRIBSYS_REPO_USERNAME }}
          password: ${{ secrets.CONTRIBSYS_REPO_PASSWORD }}
        ports:
          - '7419:7419'
        options: >-
          --health-cmd "wget -nv -t1 --spider http://localhost:7420/stats"
          --health-interval 1s
          --health-timeout 1s
          --health-retries 5

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Migrate Database
        uses: joshuaavalon/flyway-action@v3.0.0 # flyway-action 3.0.0 uses flyway v9, which should (if we do our jobs) match what's in Dockerfile.db_clean and Dockerfile.db_migrations
        with:
          url: jdbc:postgresql://postgres:5432/${{ env.POSTGRES_DB }}
          user: ${{ env.POSTGRES_USER }}
          password: ${{ env.POSTGRES_PASSWORD }}
          locations: filesystem:./migrations
        env:
          FLYWAY_SCHEMAS: public
          FLYWAY_DEFAULT_SCHEMA: public

      - name: Make MINIO Bucket
        env:
          AWS_ACCESS_KEY_ID: ${{ env.MINIO_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ env.MINIO_KEY }}
          AWS_DEFAULT_REGION: us-west-2
        run: >-
          aws --endpoint-url ${{ env.MINIO_ADDRESS }}
          s3 mb s3://${{ env.S3_BUCKET }}

      - name: Set up go
        uses: actions/setup-go@v3
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Install go modules
        run: go mod download

      - name: Set env vars with direnv
        uses: HatsuneMiku3939/direnv-action@v1

      - name: Run backend server tests
        run: |
          go test -short -count=1 -p=1 ./...

      - name: Logout Contribsys Docker Repo
        run: docker logout docker.contribsys.com
        if: always()

      # Leaving this commented out for now, but we should consider uploading these artifacts if they're
      # actually useful!
      # - name: Upload backend test artifacts
      #   uses: actions/upload-artifact@v
      #   with:
      #     name: backend-tests
      #     path: results
      #     retention-days: 14

      - name: Announce failure
        if: ${{ failure() }}
        run: |
          ./scripts/github-action-announce-broken-branch

  e2e_tests:
    runs-on: ubuntu-latest
    needs: build_docker_images
    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          ref: ${{ env.GIT_HASH }}
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          cache: yarn
      - name: Install yarn dependencies
        run: yarn install --immutable
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: docker.contribsys.com
          username: ${{ secrets.CONTRIBSYS_REPO_USERNAME }}
          password: ${{ secrets.CONTRIBSYS_REPO_PASSWORD }}
      - name: Set env vars with direnv
        uses: HatsuneMiku3939/direnv-action@v1
      - name: Add "minio" to /etc/hosts
        run: sudo echo "127.0.0.1 minio" | sudo tee -a /etc/hosts
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # # Setup Docker Caches
      # - name: Cache Docker layers (backend)
      #   uses: actions/cache@v3
      #   with:
      #     path: /tmp/.buildx-backend-cache
      #     key: ${{ runner.os }}-buildx-backend-${{ github.sha }}
      #     restore-keys: |
      #       ${{ runner.os }}-buildx-backend

      # - name: Cache Docker layers (frontend)
      #   uses: actions/cache@v3
      #   with:
      #     path: /tmp/.buildx-frontend-cache
      #     key: ${{ runner.os }}-buildx-frontend-${{ github.sha }}
      #     restore-keys: |
      #       ${{ runner.os }}-buildx-frontend

      # - name: Cache Docker layers (migrate)
      #   uses: actions/cache@v3
      #   with:
      #     path: /tmp/.buildx-migrate-cache
      #     key: ${{ runner.os }}-buildx-migrate-${{ github.sha }}
      #     restore-keys: |
      #       ${{ runner.os }}-buildx-migrate

      # # Build containers
      # - name: Build backend container
      #   uses: docker/build-push-action@v4
      #   with:
      #     load: true
      #     file: Dockerfile
      #     cache-from: type=local,src=/tmp/.buildx-backend-cache
      #     cache-to: type=local,dest=/tmp/.buildx-backend-cache,mode=max

      # - name: Build frontend container
      #   uses: docker/build-push-action@v4
      #   with:
      #     load: true
      #     file: Dockerfile.client
      #     cache-from: type=local,src=/tmp/.buildx-frontend-cache
      #     cache-to: type=local,dest=/tmp/.buildx-frontend-cache,mode=max

      # - name: Build DB migrations container
      #   uses: docker/build-push-action@v4
      #   with:
      #     load: true
      #     file: Dockerfile.db_migrations
      #     cache-from: type=local,src=/tmp/.buildx-migrate-cache
      #     cache-to: type=local,dest=/tmp/.buildx-migrate-cache,mode=max

      # Build containers
      # - name: Build backend container
      #   uses: docker/build-push-action@v4
      #   with:
      #     load: true
      #     file: Dockerfile
      #     target: dev
      #     cache-from: "type=gha,scope=${{ env.GIT_REF_NAME }}-backend"
      #     cache-to: "type=gha,mode=max,scope=${{ env.GIT_REF_NAME }}-backend"

      # - name: Build frontend container
      #   uses: docker/build-push-action@v4
      #   with:
      #     load: true
      #     file: Dockerfile.client
      #     cache-from: "type=gha,scope=${{ env.GIT_REF_NAME }}-frontend"
      #     cache-to: "type=gha,mode=max,scope=${{ env.GIT_REF_NAME }}-frontend"

      # - name: Build DB migrations container
      #   uses: docker/build-push-action@v4
      #   with:
      #     load: true
      #     file: Dockerfile.db_migrations
      #     cache-from: "type=gha,scope=${{ env.GIT_REF_NAME }}-migrate"
      #     cache-to: "type=gha,mode=max,scope=${{ env.GIT_REF_NAME }}-migrate"

      - name: Run e2e tests
        run: |
          export FLAG_SOURCE=FILE
          scripts/dev down
          scripts/dev up:ci
          echo "yarn cypress run"
      - name: Docker Compose Down
        if: always() # Always spin down the container
        run: |
          scripts/dev down

      - name: Upload cypress videos
        if: ${{ always() }}
        uses: actions/upload-artifact@v3
        with:
          name: cypress-videos
          path: cypress/videos
          retention-days: 14
      - name: Upload cypress screenshots
        if: ${{ always() }}
        uses: actions/upload-artifact@v3
        with:
          name: cypress-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore
          retention-days: 14
      - name: Announce failure
        if: ${{ failure() }}
        run: |
          ./scripts/github-action-announce-broken-branch

  lint:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          ref: ${{ env.GIT_HASH }}
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          cache: yarn
      - name: Install yarn dependencies
        run: yarn install --immutable
      - name: Set up go
        uses: actions/setup-go@v3
        with:
          go-version-file: 'go.mod'
          cache: true
      - name: Install go modules
        run: go mod download
      # go-swagger is used for pre-commit hooks
      - name: Install go-swagger
        id: install-go-swagger
        run: |
          cd /usr/local/bin
          wget -O swagger https://github.com/go-swagger/go-swagger/releases/download/v0.28.0/swagger_linux_amd64
          chmod +x swagger
      - name: Pre-commit checks
        uses: pre-commit/action@v3.0.0
        with:
          extra_args: --all-files
      - name: Announce failure
        if: ${{ failure() }}
        run: |
          ./scripts/github-action-announce-broken-branch

  client_test:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          ref: ${{ env.GIT_HASH }}
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          cache: yarn
      - name: Install yarn dependencies
        run: yarn install --immutable
      # Switch to :coverage when we have improved coverage numbers
      # TODO: https://jiraent.cms.gov/browse/EASI-1993
      # - run: yarn test:coverage
      - run: yarn test
      - name: Announce failure
        if: ${{ failure() }}
        run: |
          ./scripts/github-action-announce-broken-branch

  client_typecheck:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          ref: ${{ env.GIT_HASH }}
      - name: Set up node
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
          cache: yarn
      - name: Install yarn dependencies
        run: yarn install --immutable
      - name: Run typechecking
        id: typechecker
        # Uses GitHub environment files to store typechecker output and pass to next stage
        # see https://github.com/actions/toolkit/blob/main/docs/commands.md#environment-files
        run: |
          yarn build:ts 2>&1 | tee test.log
          result_code=${PIPESTATUS[0]}
          echo 'TS_LOG<<EOF' >> $GITHUB_ENV
          cat test.log >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV
          exit $result_code
        continue-on-error: true
